const express = require('express');
const router = express.Router();


module.exports.confige = {
    database: 'nodewowender',
    host: "localhost",
    user: "root",
    password: "",
    multipleStatements:true
}